//izvođenje zadatka
function greet(color, box) {
    console.log('Hejj ' + color + ' ' + box);
}

//izracunavanje vrijednosti 
function square(number) {
    return number * number;
}

let number = square(5);
console.log(number);